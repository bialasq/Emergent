# Copy to .dev.vars for `npm run dev` (wrangler never commits .dev.vars)
# Min. 16 znaków — tak samo w produkcji: `npx wrangler secret put JWT_SECRET` (bez pliku .dev.vars)
JWT_SECRET=replace-with-at-least-16-chars-local-only
