{
  "name": "dungeon-of-echoes-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:local": "wrangler d1 execute dungeon_of_echoes --local --file=./schema.sql",
    "d1:remote": "wrangler d1 execute dungeon_of_echoes --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
