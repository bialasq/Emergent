# Używane przez `npm run build` — gra woła API Workera (CORS + Bearer).
# Dla własnej domeny Workera podmień URL i zsynchronizuj ALLOWED_ORIGINS w cf-worker/wrangler.toml.
REACT_APP_BACKEND_URL=https://dungeon-of-echoes-api.dungeonofechoes.workers.dev
