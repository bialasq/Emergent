# Build-time: ustaw w Cloudflare Pages → Settings → Environment variables (Production + Preview),
# żeby buildy z Git-a miały poprawny API (CLI może ustawiać zmienną tylko lokalnie przy `npm run build`).
# Bez końcowego „/”.
REACT_APP_BACKEND_URL=https://dungeon-of-echoes-api.dungeonofechoes.workers.dev
